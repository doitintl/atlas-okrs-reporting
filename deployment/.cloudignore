# Cloud Build ignore file - excludes files from build context
# This optimizes build performance and reduces security risks

# === VERSION CONTROL ===
.git/
.gitignore
.gitattributes
.gitmodules

# === DOCUMENTATION ===
README.md
CLOUD_RUN.md
CHANGELOG.md
*.md
docs/
documentation/

# === CONFIGURATION FILES ===
# Note: config.env is needed by cloudbuild.yaml, so we don't exclude it
*.env.local
*.env.development
*.env.production
*.env.staging

# === DEVELOPMENT FILES ===
.vscode/
.idea/
.vs/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# === PYTHON DEVELOPMENT ===
.venv/
venv/
env/
.env/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.py[cod]
*$py.class
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/
.mypy_cache/
.dmypy.json
dmypy.json

# === DEPENDENCY MANAGEMENT ===
# Keep uv.lock but exclude other lock files
pip-requirements.txt
requirements-dev.txt
Pipfile
Pipfile.lock
poetry.lock
# Keep: uv.lock (needed for uv)
# Keep: pyproject.toml (needed for dependencies)

# === BUILD ARTIFACTS ===
build/
dist/
*.egg-info/
wheels/
*.egg
.eggs/

# === LOGS ===
*.log
logs/
log/

# === TEMPORARY FILES ===
tmp/
temp/
*.tmp
*.temp
.cache/

# === DATA FILES ===
data/
scraped/
data_snapshots/
# Note: teams.csv is needed by the application, so we don't exclude it
# Note: config.env is needed by cloudbuild.yaml, so we don't exclude it

# === BACKUP FILES ===
*.bak
*.backup
*.old
*~

# === SYSTEM FILES ===
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# === DEPLOYMENT SCRIPTS ===
# We want to exclude these from build context as they're not needed in container
setup_cloud_infrastructure.sh
deploy.sh

# === TESTING ===
tests/
test/
*_test.py
*_test.go
*.test
coverage.xml
nosetests.xml
pytest.ini
tox.ini

# === SECRETS AND CREDENTIALS ===
# Never include any credential files
*credentials*
*secrets*
*.key
*.pem
*.crt
*.p12
*.pfx
serviceaccount.json
service-account.json
google-credentials.json
gcloud-credentials.json

# === CLOUD BUILD SPECIFIC ===
# Build configuration files are handled separately
# Keep: cloudbuild.yaml (needed for build)
# Keep: Dockerfile (needed for build)
# Keep: .dockerignore (needed for Docker build)

# === HELPERS AND TOOLS ===
# These are excluded as they're not needed in the deployed application
helpers/
tools/
scripts/
utilities/

# === HEALTH CHECK FILES ===
healthcheck_ignore_files.py
fix_ignore_files.py
